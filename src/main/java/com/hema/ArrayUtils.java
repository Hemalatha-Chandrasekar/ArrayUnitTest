package com.hema;

public class  ArrayUtils {
    public int[] sortArray(int[] arr) {
        java.util.Arrays.sort(arr); // Use java.util.Arrays.sort to actually sort the array
        return arr;
    }
}
